# LusyChat 角色卡補 Tag · 首批審核

這是一個只讀的靜態審核頁，展示 50 個正式庫公開角色的 AI 補 Tag 初篩結果。

- 原 Tag 完整保留，只展示新增建議。
- 建議沒有寫入正式庫。
- 候選庫包括 137 個已啟用 Tag，以及 2 個新增但暫未啟用的 Tag。
- 頁面使用 `zh-TW` 多語言內容；角色專有名稱沿用原名稱。

打開 `index.html` 即可查看，線上版本由 GitHub Pages 發布。
